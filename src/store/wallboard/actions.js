export default {
  setFilter ({ commit }, { filter, value }) {
    commit('SET_FILTER', { filter, value })
  }
  // fetchSummary
  // fetchUsers
  // fetchQueuedCalls
  // fetchLiveCalls
  // fetchParkedCalls
}
