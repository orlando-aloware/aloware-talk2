export const RINGING = 'ringing' // The call is currently ringing (relative to us)
export const GREETING = 'greeting' // Caller is hearing the greeting message
export const QUEUED = 'queued' // the call is in our internal queue.
export const TRANSFERRING = 'transferring' // We are dialing the user
export const WHISPERING = 'whispering' // Callee picked up and is hearing the whisper message.
export const INPROGRESS = 'in-progress' // The call was answered and is currently in progress.
export const COMPLETED = 'completed'
